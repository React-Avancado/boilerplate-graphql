import styled from 'styled-components'

export const Wrapper = styled.main`
  margin: auto;
  max-width: 300px;

  img {
    border-radius: 50%;
  }
`

export const Title = styled.h2`
  font-size: 18px;
  margin-top: 8px;
  text-align: center;
`
